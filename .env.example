REGISTRY=agent-hub
AGENT_TAG=dev
# AGENT_TAG is a temporary override for one-off builds/tests.
# The normal default tag for each agent should live in agents/<name>/index.yaml.
BASE_IMAGE=ubuntu:24.04
