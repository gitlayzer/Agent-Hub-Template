REGISTRY=agent-hub
BASE_NAMESPACE=base
BASE_TAG=dev
AGENT_TAG=dev
# AGENT_TAG is a temporary override for one-off builds/tests.
# The normal default tag for each agent should live in agents/<name>/agent.yaml.
PUSH=false
DEFAULT_BASE=ubuntu
