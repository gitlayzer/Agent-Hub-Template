name: openclaw
image:
  repository: agent-hub/openclaw
  tag: dev
build:
  args:
    OPENCLAW_VERSION: 2026.4.14
    NODE_VERSION: 24.14.1
smoke_test:
  - --version
metadata:
  description: Real OpenClaw runtime image built from the official npm package and upstream project.
  category: platform-agent
