name: change-me
image:
  repository: agent-hub/change-me
  tag: dev
build:
  args: {}
smoke_test:
  - shell
  - -lc
  - test -x /opt/change-me/bin/change-me-run
metadata:
  description: Template definition for a new agent image.
  category: custom-agent
